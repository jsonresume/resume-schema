{
  "name": "resume-schema",
  "version": "0.1.3",
  "description": "JSON Resume Schema",
  "main": "validator.js",
  "scripts": {
    "test": "scripts/test.sh",
    "format-schema": "scripts/format-schema.sh",
    "lint": "eslint . --ext .json,.js",
    "test-units": "tape **/*.spec.js | tap-spec",
    "preversion": "npm test",
    "postversion": "git push --follow-tags"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/jsonresume/resume-schema.git"
  },
  "license": "BSD-2-Clause",
  "bugs": {
    "url": "https://github.com/jsonresume/resume-schema/issues"
  },
  "files": [
    "sample.resume.json",
    "schema.json",
    "validator.js"
  ],
  "dependencies": {
    "z-schema": "^4.2.2"
  },
  "devDependencies": {
    "ajv-cli": "^3.1.0",
    "babel-eslint": "^10.1.0",
    "eslint": "^6.8.0",
    "eslint-plugin-editorconfig": "^2.1.2",
    "eslint-plugin-json": "^2.1.1",
    "json-metaschema": "^1.3.0",
    "jsonlint-cli": "^1.0.1",
    "tap-spec": "^5.0.0",
    "tape": "^4.13.2"
  },
  "engines": {
    "node": ">=10"
  }
}
